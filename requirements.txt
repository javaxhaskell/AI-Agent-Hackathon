# Vercel serverless build ONLY — the minimal subset the console (api/index.py)
# needs. Local development uses pyproject.toml, which is the full dependency set
# (pandas/numpy/pyyaml/sse-starlette/uvicorn + the [dev]/[agents] extras). This
# file is deliberately small so the serverless bundle stays well under Vercel's
# size limit and builds fast. starlette / anyio / pydantic-core resolve as deps.
fastapi>=0.115
pydantic>=2.9
httpx>=0.27
